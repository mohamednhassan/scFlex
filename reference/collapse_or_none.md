# Collapse values for display

Collapses a vector into a comma-separated string and returns `"None"`
for null or empty input.

## Usage

``` r
collapse_or_none(x)
```

## Arguments

- x:

  A vector-like object to collapse.

## Value

A single character string.
